services:
  wyoming-whisper:
    build:
      context: ./whisper
      dockerfile: Dockerfile
      args:
        HSA_OVERRIDE_GFX_VERSION: ${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
        # Build for multiple GPU architectures (RDNA 2 and RDNA 3)
        AMDGPU_TARGETS: gfx1151
        CMAKE_HIP_ARCHITECTURES: gfx1151
    container_name: wyoming-whisper-rocm
    restart: unless-stopped

    # GPU device access - required for ROCm
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri

    # Security options for GPU access
    security_opt:
      - seccomp:unconfined

    # Group access for GPU
    group_add:
      - video
      - render

    # Environment variables
    environment:
      - HSA_OVERRIDE_GFX_VERSION=${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
      - HIP_VISIBLE_DEVICES=0
      - ROCM_VERSION=7.2.1
      - ROCM_PATH=/opt/rocm
      - LD_LIBRARY_PATH=/usr/local/lib:/opt/rocm/lib:/opt/rocm/lib/llvm/lib
      - CTRANSLATE2_ROOT=/usr/local
      # Whisper model configuration
      - WHISPER_MODEL=${WHISPER_MODEL:-medium}
      - WHISPER_COMPUTE_TYPE=${WHISPER_COMPUTE_TYPE:-float16}
      - WHISPER_BEAM_SIZE=${WHISPER_BEAM_SIZE:-5}
      - WHISPER_DEBUG=${WHISPER_DEBUG:-false}

    # Port for Wyoming protocol
    ports:
      - "10300:10300"

    # Volume for model persistence
    volumes:
      - ./data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10300)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # wyoming-piper: (Commented out - migrated to Qwen3-TTS)
  # wyoming-piper:
  #   build:
  #     context: ./piper
  #     dockerfile: Dockerfile
  #     args:
  #       HSA_OVERRIDE_GFX_VERSION: ${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
  #       # Build for multiple GPU architectures (RDNA 2 and RDNA 3)
  #       AMDGPU_TARGETS: gfx1151
  #   container_name: wyoming-piper-rocm
  #   restart: unless-stopped
  #
  #   # GPU device access - required for ROCm
  #   devices:
  #     - /dev/kfd:/dev/kfd
  #     - /dev/dri:/dev/dri
  #
  #   # Security options for GPU access
  #   security_opt:
  #     - seccomp:unconfined
  #
  #   # Group access for GPU
  #   group_add:
  #     - video
  #     - render
  #
  #   # Environment variables
  #   environment:
  #     - HSA_OVERRIDE_GFX_VERSION=${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
  #     - HIP_VISIBLE_DEVICES=0
  #     - ROCM_VERSION=7.1.1
  #     - ROCM_PATH=/opt/rocm
  #     - LD_LIBRARY_PATH=/usr/local/lib:/opt/rocm/lib:/opt/rocm/lib/llvm/lib
  #     # ONNX Runtime threading - configured dynamically by piper_wrapper.py
  #     # Piper voice configuration
  #     - PIPER_VOICE=${PIPER_VOICE:-en_US-lessac-medium}
  #     - PIPER_LENGTH_SCALE=${PIPER_LENGTH_SCALE:-1.0}
  #     - PIPER_NOISE_SCALE=${PIPER_NOISE_SCALE:-0.667}
  #     - PIPER_NOISE_W=${PIPER_NOISE_W:-0.8}
  #     - PIPER_DEBUG=${PIPER_DEBUG:-false}
  #
  #   # Port for Wyoming protocol
  #   ports:
  #     - "10200:10200"
  #
  #   # Volume for model persistence
  #   volumes:
  #     - ./piper-data:/data
  #
  #   # Health check
  #   healthcheck:
  #     test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10200)); s.close()"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 60s

  wyoming-qwen-tts:
    build:
      context: ./qwen3-tts
      dockerfile: Dockerfile
      args:
        HSA_OVERRIDE_GFX_VERSION: ${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
    container_name: wyoming-qwen-tts-rocm
    restart: unless-stopped

    # GPU device access - required for ROCm
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri

    # Security options for GPU access
    security_opt:
      - seccomp:unconfined

    # Group access for GPU
    group_add:
      - video
      - render

    # Environment variables
    environment:
      - HSA_OVERRIDE_GFX_VERSION=${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
      - HIP_VISIBLE_DEVICES=0
      - ROCM_VERSION=7.2.1
      - ROCM_PATH=/opt/rocm
      - LD_LIBRARY_PATH=/usr/local/lib:/opt/rocm/lib:/opt/rocm/lib/llvm/lib
      # Enable experimental ROCm SDPA optimizations for better performance
      - TORCH_ROCM_AOTRITON_ENABLE_EXPERIMENTAL=1
      # MIOpen performance tuning - fix workspace allocation warnings
      - MIOPEN_USER_DB_PATH=/tmp/miopen-cache
      - MIOPEN_CUSTOM_CACHE_DIR=/tmp/miopen-cache
      - MIOPEN_FIND_MODE=1
      # Hugging Face cache configuration
      - HF_HOME=/data/hf_cache
      - TRANSFORMERS_CACHE=/data/hf_cache
      - HUGGINGFACE_HUB_CACHE=/data/hf_cache/hub
      # Qwen3-TTS configuration
      - QWEN_MODEL=${QWEN_MODEL:-Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice}
      - QWEN_VOICE_INSTRUCT=${QWEN_VOICE_INSTRUCT:-}
      - QWEN_SPEAKER=${QWEN_SPEAKER:-Ryan}
      - QWEN_LANGUAGE=${QWEN_LANGUAGE:-Auto}
      - QWEN_DEVICE=${QWEN_DEVICE:-cuda:0}
      - QWEN_DTYPE=${QWEN_DTYPE:-bfloat16}
      - QWEN_FLASH_ATTENTION=${QWEN_FLASH_ATTENTION:-true}
      - QWEN_SAMPLES_PER_CHUNK=${QWEN_SAMPLES_PER_CHUNK:-1024}
      - QWEN_CACHE_DIR=/data/models
      - QWEN_DEBUG=${QWEN_DEBUG:-true}

    # Port for Wyoming protocol
    ports:
      - "10200:10200"

    # Volume for model persistence
    volumes:
      - ./qwen-data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10200)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

  wyoming-chatterbox-turbo:
    build:
      context: ./chatterbox-turbo
      dockerfile: Dockerfile
      args:
        HSA_OVERRIDE_GFX_VERSION: ${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
    container_name: wyoming-chatterbox-turbo-rocm
    restart: unless-stopped

    # GPU device access - required for ROCm
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri

    # Security options for GPU access
    security_opt:
      - seccomp:unconfined

    # Group access for GPU
    group_add:
      - video
      - render

    # Environment variables
    environment:
      - HSA_OVERRIDE_GFX_VERSION=${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
      - HIP_VISIBLE_DEVICES=0
      - ROCM_VERSION=7.2.1
      - ROCM_PATH=/opt/rocm
      - LD_LIBRARY_PATH=/usr/local/lib:/opt/rocm/lib:/opt/rocm/lib/llvm/lib
      # Enable experimental ROCm SDPA optimizations for better performance
      - TORCH_ROCM_AOTRITON_ENABLE_EXPERIMENTAL=1
      # MIOpen performance tuning - fix workspace allocation warnings
      - MIOPEN_USER_DB_PATH=/tmp/miopen-cache
      - MIOPEN_CUSTOM_CACHE_DIR=/tmp/miopen-cache
      - MIOPEN_FIND_MODE=1
      # Hugging Face cache configuration
      - HF_HOME=/data/hf_cache
      - TRANSFORMERS_CACHE=/data/hf_cache
      - HUGGINGFACE_HUB_CACHE=/data/hf_cache/hub
      - HF_TOKEN=${HF_TOKEN}
      # Chatterbox Turbo configuration
      - CHATTERBOX_DEVICE=${CHATTERBOX_DEVICE:-cuda:0}
      - CHATTERBOX_SAMPLES_PER_CHUNK=${CHATTERBOX_SAMPLES_PER_CHUNK:-1024}
      - CHATTERBOX_CACHE_DIR=/data/models
      - CHATTERBOX_DEBUG=${CHATTERBOX_DEBUG:-true}

    # Port for Wyoming protocol (using 10201 to avoid conflict with qwen)
    ports:
      - "10201:10200"

    # Volume for model persistence
    volumes:
      - ./chatterbox-data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10200)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  wyoming-pocket-tts:
    build:
      context: ./pocket-tts
      dockerfile: Dockerfile
      args:
        HSA_OVERRIDE_GFX_VERSION: ${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
    container_name: wyoming-pocket-tts-rocm
    restart: unless-stopped

    # GPU device access - required for ROCm base image
    # Note: Pocket TTS is CPU-only and doesn't benefit from GPU
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri

    # Security options for GPU access
    security_opt:
      - seccomp:unconfined

    # Group access for GPU
    group_add:
      - video
      - render

    # Environment variables
    environment:
      - HSA_OVERRIDE_GFX_VERSION=${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
      - ROCM_VERSION=7.2.1
      - ROCM_PATH=/opt/rocm
      - LD_LIBRARY_PATH=/usr/local/lib:/opt/rocm/lib:/opt/rocm/lib/llvm/lib
      # Hugging Face cache configuration
      - HF_HOME=/data/hf_cache
      - TRANSFORMERS_CACHE=/data/hf_cache
      - HUGGINGFACE_HUB_CACHE=/data/hf_cache/hub
      # Pocket TTS configuration
      # Built-in voices: alba, marius, javert, jean, fantine, cosette, eponine, azelma
      # For voice cloning, use HF URLs or .wav file paths (requires gated model access)
      - POCKET_VOICE=${POCKET_VOICE:-fantine}
      - POCKET_CACHE_DIR=/data/models
      - POCKET_DEBUG=${POCKET_DEBUG:-true}

    # Port for Wyoming protocol (using 10202 to avoid conflicts)
    ports:
      - "10202:10202"

    # Volume for model persistence
    volumes:
      - ./pocket-data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10202)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  wyoming-kokoro-tts:
    build:
      context: ./kokoro
      dockerfile: Dockerfile
    container_name: wyoming-kokoro-tts-rocm
    restart: unless-stopped

    # No GPU devices - proxies to external API

    # Environment variables
    environment:
      - KOKORO_API_URL=${KOKORO_API_URL:-http://10.0.3.23:8880/v1}
      - KOKORO_VOICE=${KOKORO_VOICE:-af_bella}
      - KOKORO_SPEED=${KOKORO_SPEED:-1.0}
      - KOKORO_TIMEOUT=${KOKORO_TIMEOUT:-30}
      - KOKORO_DEBUG=${KOKORO_DEBUG:-false}

    # Port for Wyoming protocol (using 10203 to avoid conflicts)
    ports:
      - "10203:10203"

    # Volume for potential caching needs
    volumes:
      - ./kokoro-data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10203)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  wyoming-moonshine:
    build:
      context: ./moonshine
      dockerfile: Dockerfile
    container_name: wyoming-moonshine
    restart: unless-stopped

    # Environment variables
    environment:
      # Hugging Face cache configuration
      - HF_HOME=/data/hf_cache
      - HUGGINGFACE_HUB_CACHE=/data/hf_cache/hub
      # Moonshine configuration
      # Models: moonshine/tiny (27M params) or moonshine/base (62M params)
      - MOONSHINE_MODEL=${MOONSHINE_MODEL:-moonshine/tiny}
      - MOONSHINE_DEBUG=${MOONSHINE_DEBUG:-false}

    # Port for Wyoming protocol (10302 for STT)
    ports:
      - "10302:10302"

    # Volume for model persistence
    volumes:
      - ./moonshine-data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10302)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  wyoming-voxtral:
    build:
      context: ./voxtral
      dockerfile: Dockerfile
      args:
        HSA_OVERRIDE_GFX_VERSION: ${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
    container_name: wyoming-voxtral-rocm
    restart: unless-stopped

    # GPU device access - required for vLLM with ROCm
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri

    # Security options for GPU access
    security_opt:
      - seccomp:unconfined

    # Group access for GPU
    group_add:
      - video
      - render

    # Environment variables
    environment:
      - HSA_OVERRIDE_GFX_VERSION=${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
      - HIP_VISIBLE_DEVICES=0
      - ROCM_VERSION=7.2.1
      - ROCM_PATH=/opt/rocm
      - LD_LIBRARY_PATH=/usr/local/lib:/opt/rocm/lib:/opt/rocm/lib/llvm/lib
      # ROCm/CUDA environment (ROCm uses CUDA API compatibility)
      - CUDA_VISIBLE_DEVICES=0
      - ROCR_VISIBLE_DEVICES=0
      - GPU_DEVICE_ORDINAL=0
      # vLLM configuration
      - VLLM_DISABLE_COMPILE_CACHE=1
      - VLLM_USE_RAY_COMPILED_DAG=0
      - VLLM_WORKER_MULTIPROC_METHOD=spawn
      - VLLM_USE_TRITON_FLASH_ATTN=0
      - VLLM_ATTENTION_BACKEND=XFORMERS
      # Prevent Ray from interfering with ROCm device visibility
      - RAY_EXPERIMENTAL_NOSET_ROCR_VISIBLE_DEVICES=1
      - RAY_EXPERIMENTAL_NOSET_HIP_VISIBLE_DEVICES=1
      # Hugging Face cache configuration
      - HF_HOME=/data/hf_cache
      - TRANSFORMERS_CACHE=/data/hf_cache
      - HUGGINGFACE_HUB_CACHE=/data/hf_cache/hub
      # Voxtral configuration
      - VOXTRAL_MODEL=${VOXTRAL_MODEL:-mistralai/Voxtral-Mini-4B-Realtime-2602}
      - VOXTRAL_LANGUAGE=${VOXTRAL_LANGUAGE:-en}
      - VOXTRAL_GPU_MEMORY=${VOXTRAL_GPU_MEMORY:-0.9}
      - VOXTRAL_DEBUG=${VOXTRAL_DEBUG:-false}

    # Port for Wyoming protocol (using 10301 for STT)
    ports:
      - "10301:10301"

    # Volume for model persistence
    volumes:
      - ./voxtral-data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10301)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 180s  # vLLM model loading can take time

  wyoming-parakeet:
    build:
      context: ./parakeet
      dockerfile: Dockerfile
      args:
        HSA_OVERRIDE_GFX_VERSION: ${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
    container_name: wyoming-parakeet-rocm
    restart: unless-stopped

    # GPU device access - required for ROCm
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri

    # Security options for GPU access
    security_opt:
      - seccomp:unconfined

    # Group access for GPU
    group_add:
      - video
      - render

    # Environment variables
    environment:
      - HSA_OVERRIDE_GFX_VERSION=${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
      - HIP_VISIBLE_DEVICES=0
      - ROCM_VERSION=7.2.1
      - ROCM_PATH=/opt/rocm
      - LD_LIBRARY_PATH=/usr/local/lib:/opt/rocm/lib:/opt/rocm/lib/llvm/lib
      # Enable experimental ROCm SDPA optimizations for better performance
      - TORCH_ROCM_AOTRITON_ENABLE_EXPERIMENTAL=1
      # MIOpen performance tuning
      - MIOPEN_USER_DB_PATH=/tmp/miopen-cache
      - MIOPEN_CUSTOM_CACHE_DIR=/tmp/miopen-cache
      - MIOPEN_FIND_MODE=1
      # Hugging Face cache configuration
      - HF_HOME=/data/hf_cache
      - TRANSFORMERS_CACHE=/data/hf_cache
      - HUGGINGFACE_HUB_CACHE=/data/hf_cache/hub
      # Parakeet configuration
      - PARAKEET_MODEL=${PARAKEET_MODEL:-nvidia/parakeet-tdt-0.6b-v3}
      - PARAKEET_DEVICE=${PARAKEET_DEVICE:-cuda:0}
      - PARAKEET_DEBUG=${PARAKEET_DEBUG:-false}

    # Port for Wyoming protocol (10303 for STT)
    ports:
      - "10303:10303"
      - "${PARAKEET_OPENAI_PORT:-10313}:8080"

    # Volume for model persistence
    volumes:
      - ./parakeet-data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10303)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

  wyoming-granite:
    build:
      context: ./granite
      dockerfile: Dockerfile
      args:
        HSA_OVERRIDE_GFX_VERSION: ${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
        AMDGPU_TARGETS: gfx1151
        CMAKE_HIP_ARCHITECTURES: gfx1151
    container_name: wyoming-granite-rocm
    restart: unless-stopped

    # GPU device access - required for ROCm
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri

    # Security options for GPU access
    security_opt:
      - seccomp:unconfined

    # Group access for GPU
    group_add:
      - video
      - render

    # Environment variables
    environment:
      - HSA_OVERRIDE_GFX_VERSION=${HSA_OVERRIDE_GFX_VERSION:-11.5.1}
      - HIP_VISIBLE_DEVICES=0
      - ROCM_VERSION=7.2.1
      - ROCM_PATH=/opt/rocm
      - LD_LIBRARY_PATH=/usr/local/lib:/opt/rocm/lib:/opt/rocm/lib/llvm/lib
      # Enable experimental ROCm SDPA optimizations for better performance
      - TORCH_ROCM_AOTRITON_ENABLE_EXPERIMENTAL=1
      # MIOpen performance tuning
      - MIOPEN_USER_DB_PATH=/tmp/miopen-cache
      - MIOPEN_CUSTOM_CACHE_DIR=/tmp/miopen-cache
      - MIOPEN_FIND_MODE=1
      # Hugging Face cache configuration
      - HF_HOME=/data/hf_cache
      - TRANSFORMERS_CACHE=/data/hf_cache
      - HUGGINGFACE_HUB_CACHE=/data/hf_cache/hub
      # Granite Speech configuration
      - GRANITE_MODEL=${GRANITE_MODEL:-ibm-granite/granite-speech-4.1-2b-nar}
      - GRANITE_DEBUG=${GRANITE_DEBUG:-false}

    # Port for Wyoming protocol (10304 for STT)
    ports:
      - "10304:10304"

    # Volume for model persistence
    volumes:
      - ./granite-data:/data

    # Health check
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost',10304)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
